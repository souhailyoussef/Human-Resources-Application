import { SearchByRubriqueNamePipe } from './search-by-rubrique-name.pipe';

describe('SearchByRubriqueNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByRubriqueNamePipe();
    expect(pipe).toBeTruthy();
  });
});
